import AppShell from "@/app/annix-sentinel/components/AppShell";

export default function HealthReportLayout({ children }: { children: React.ReactNode }) {
  return <AppShell>{children}</AppShell>;
}
