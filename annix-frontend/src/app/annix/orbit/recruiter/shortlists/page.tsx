import { RecruiterPlaceholder } from "../components/RecruiterPlaceholder";

export default function RecruiterShortlistsPage() {
  return (
    <RecruiterPlaceholder
      title="Shortlists"
      description="Package candidates for a client — rank them, add recruiter notes, generate a client summary, and export or share via a secure link."
      phase="Phase 2"
    />
  );
}
