import { RecruiterPlaceholder } from "../components/RecruiterPlaceholder";

export default function RecruiterCompliancePage() {
  return (
    <RecruiterPlaceholder
      title="Compliance"
      description="Per-industry document templates (ID, qualifications, certificates, licences, clearances) with complete / missing / expiring / expired status per candidate."
      phase="Phase 3"
    />
  );
}
