import { RecruiterPlaceholder } from "../components/RecruiterPlaceholder";

export default function RecruiterSettingsPage() {
  return (
    <RecruiterPlaceholder
      title="Settings"
      description="Agency profile, team management with recruiter roles (Owner / Manager / Recruiter / Assistant), and candidate data-ownership defaults (private / agency / public)."
      phase="Phase 2"
    />
  );
}
