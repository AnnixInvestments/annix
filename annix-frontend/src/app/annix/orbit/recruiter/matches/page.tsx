import { RecruiterPlaceholder } from "../components/RecruiterPlaceholder";

export default function RecruiterMatchesPage() {
  return (
    <RecruiterPlaceholder
      title="Matches"
      description="AI matches between jobs and candidates, both directions, each with a plain-English explanation of why it fits and what's missing — not just a score."
      phase="Phase 2"
    />
  );
}
