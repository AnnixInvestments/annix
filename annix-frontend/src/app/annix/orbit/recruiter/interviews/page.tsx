import { RecruiterPlaceholder } from "../components/RecruiterPlaceholder";

export default function RecruiterInterviewsPage() {
  return (
    <RecruiterPlaceholder
      title="Interviews"
      description="Calendar and list of candidate interviews across rounds — send prep, generate questions, and capture candidate and client feedback."
      phase="Phase 3"
    />
  );
}
