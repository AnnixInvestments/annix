import { RecruiterPlaceholder } from "../components/RecruiterPlaceholder";

export default function RecruiterJobsPage() {
  return (
    <RecruiterPlaceholder
      title="Jobs"
      description="Client vacancies you're working — with AI matches, shortlist progress, submissions and interview pipeline per role."
      phase="Phase 2"
    />
  );
}
