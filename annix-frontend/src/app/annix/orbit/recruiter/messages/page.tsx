import { RecruiterPlaceholder } from "../components/RecruiterPlaceholder";

export default function RecruiterMessagesPage() {
  return (
    <RecruiterPlaceholder
      title="Messages"
      description="Candidate and client communication with AI templates (request documents, invite to interview, send shortlist, follow up, polite reject). Email and WhatsApp to start."
      phase="a later phase"
    />
  );
}
