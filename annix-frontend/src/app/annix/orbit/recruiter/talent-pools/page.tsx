import { RecruiterPlaceholder } from "../components/RecruiterPlaceholder";

export default function RecruiterTalentPoolsPage() {
  return (
    <RecruiterPlaceholder
      title="Talent Pools"
      description="Named candidate groups (e.g. Available Immediately, Cape Town, Boilermakers) you can match to a vacancy, bulk-message, or share as a shortlist."
      phase="Phase 3"
    />
  );
}
